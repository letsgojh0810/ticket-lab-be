spring.application.name=ticket

# [Database Setup]
spring.datasource.url=jdbc:mysql://localhost:3306/ticket_db?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# [JPA / Hibernate]
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# [Redis Setup]
spring.data.redis.host=localhost
spring.data.redis.port=6379

# [Kafka Producer Setup]
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer

# [Logging]
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Gmail SMTP 설정 (현재 비활성화 - 로그로 대체)
#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=honeyguardians@gmail.com
#spring.mail.password=your-app-password-here

# SMTP 추가 설정
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true
#spring.mail.properties.mail.smtp.starttls.required=true

# 타임아웃 설정
#spring.mail.properties.mail.smtp.timeout=5000
#spring.mail.properties.mail.smtp.connectiontimeout=5000
#spring.mail.properties.mail.smtp.writetimeout=5000

# [JWT]
jwt.secret=ticket-lab-jwt-secret-key-must-be-at-least-256-bits-long-for-hs256
jwt.access-token-expiry=900000
jwt.refresh-token-expiry=604800000

# [Actuator & Prometheus]
management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.endpoint.health.show-details=always
management.endpoint.prometheus.enabled=true
management.metrics.tags.application=${spring.application.name}

# PG Simulator
pg.base-url=http://localhost:8082
